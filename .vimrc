" Enable syntax highlighting
syntax on

" Enable mouse support
set mouse=a

"""""
""""" Line Numbers
"""""
" Show line numbers
set number
" Show relative line numbers for all lines except the current
set relativenumber

"""""
""""" Indenting
"""""
" Set the number of spaces a tab counts for
set tabstop=4
" Set the number of spaces to use for each step of (auto)indent
set shiftwidth=4
" Set the number of spaces to use when using the reindent operations (<< and >>)
set softtabstop=4
" Enable smart indenting
set smartindent
" Enable auto indenting
set autoindent

" Highlight search results
set hlsearch

" Enable incremental search
set incsearch

" Show matching brackets
set showmatch

" Enable file type detection
filetype on
filetype plugin on
filetype indent on

" Set color scheme (replace 'desert' with your preferred scheme)
"colorscheme desert
